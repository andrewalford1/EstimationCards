import React from 'react'
import { Card, CardContent } from '@material-ui/core'

export default () => {

    return (
<Card variant="outlined">
    <CardContent>
        Front
    </CardContent>
</Card>
    )
}